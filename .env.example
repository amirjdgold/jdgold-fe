# Leave empty for local Vite dev (uses proxy to http://localhost:3001).
# In Vercel, set this before building to your backend origin, no trailing slash.
# Vite embeds VITE_* variables into the static bundle at build time.
# Example: https://jdg-web-backend.vercel.app
VITE_API_URL=
